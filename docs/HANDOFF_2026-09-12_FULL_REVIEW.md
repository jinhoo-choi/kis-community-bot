# 전체 구조 개선 인수인계 (2026-09-12)

Claude 후속 검토용 문서입니다. 기준 브랜치는 `main`, 검토 시작점은
`275acbe`, 이 문서 작성 직전 구현 끝점은 `0c2a01f`입니다.

기존 비용·시간 최소 변경 1~4는
[`CHANGELOG_2026-09-12_COST_TIME.md`](CHANGELOG_2026-09-12_COST_TIME.md)에
기록돼 있습니다. 이번 후속 작업은 그 변경을 실제 전체 파이프라인 기준으로
재검토하면서 정확성, 실패 처리, 소재 품질, 캐시, 중복 제거, 계측까지 확장한 것입니다.

## 변경 순서와 커밋

| 순서 | 커밋 | 변경 | 확인하려는 불변식 |
|---:|---|---|---|
| 0 | `275acbe` | Batch 기본 비활성화, Gemini circuit breaker, 단계적 생성, 수집 이중 증폭 제거 | 600초 대기·중복 호출과 전량 선생성을 없앤다 |
| 1 | `f722dfc` | 심사 응답 스키마·점수 범위 검증, 미심사 글 fail-closed | `score=None`인 글은 절대 배포하지 않는다 |
| 2 | `6fcddc0` | 수집 상한과 생성 상한 분리, 유형별 기대 수율, 첫 묶음 유형 혼합 | 단계적 생성을 해도 목표 50건 후보가 앞단에서 잘리지 않는다 |
| 3 | `9e0937a` | 텔레그램 성공 글만 상태·통계 반영, 상태 파일 원자 저장, Actions 동시 실행 차단 | 전송 실패 글을 게시 완료로 기록하지 않는다 |
| 4 | `16f34eb` | DART 상세행을 정확한 접수번호에만 결합, 전 시장 조회, 주말 조회일 수정 | 다른 공시의 수치를 붙이지 않는다 |
| 5 | `9848922` | 유의미한 derived fact만 소재로 인정, 선택 밖 claim 제거, 관계값 사용 검증 | 관계어만 넣은 raw 수치 나열은 통과하지 않는다 |
| 6 | `34bf6b1` | Gemini 일시 제한/결제 오류 분리, 검색 근거 URL 보존, 오류 캐시 금지, 실패 작성 재할당, Claude 동기 병렬화 | 장애를 `NONE`으로 캐시하지 않고 유효한 다른 provider를 쓴다 |
| 7 | `d12fb35` | 장전 확정 시세 캐시 선사용, 휴장·장애 시 최근 캐시 fallback | 정상 거래일 최신 데이터보다 오래된 캐시를 먼저 쓰지 않는다 |
| 8 | `0951701` | 사건 dedup에 날짜 추가, 제목 유사도를 종목별로 제한, 한경 리포트 발간일 보존 | 다음 날 새 공시나 타 종목의 같은 제목을 중복으로 버리지 않는다 |
| 9 | `8935fd8` | 섹터 근거 없는 정책·테마 글의 임의 대형주 배정 제거 | 무관한 종목방에 글을 올리지 않는다 |
| 10 | `793cd85` | 정책 RSS 48시간 신선도, poll 질문형 계약·결과 검증 | 오래된 뉴스와 질문 없는 발제글을 배포하지 않는다 |
| 11 | `0c2a01f` | 후보→시도→필터→심사→전송 유형별 퍼널 계측 | 비용 절감과 실제 수율을 추정이 아니라 로그로 확인한다 |

## 현재 실행 흐름

```text
후보 수집
  → 필요한 비-flow 항목만 검색 보강
  → 하드 게이트·글감 게이트
  → 종목 귀속·게시판 매핑
  → 다축 dedup
  → 유형 혼합 순서로 100건씩 생성
  → deterministic validator
  → 교차 LLM 심사
  → 배포 상한 판정
  → 50건 확보 시 다음 묶음 생략
  → 텔레그램 성공분만 상태·통계 저장
```

## 중요한 동작 변경

### 실패 처리

- 심사 모델이 없거나 JSON이 깨졌거나 1~5 범위를 벗어나면 배포 보류입니다.
- Gemini의 일시적 `429/rate limit`은 최대 3회 재시도하고, 결제·크레딧 오류만
  같은 실행 전체에서 circuit breaker를 엽니다.
- 보강 호출 오류는 캐시에 저장하지 않습니다. 검색 결과 `NONE`이 정상 응답으로
  확인된 경우만 `status=none`으로 캐시합니다.
- 초기 작성 provider 실패분은 사용 가능한 다른 provider로 한 번 재할당합니다.
- 텔레그램 부분 전송 시 성공분만 `seen`에 기록하고, 실행은 실패로 끝내 재확인을 요구합니다.

### 사실 정확성

- DART 상세 API 결과는 목록의 `rcept_no`와 정확히 일치하는 행만 사용합니다.
- `ssl_at`을 상장예정일로 읽던 잘못된 매핑을 제거했습니다.
- 검색 보강을 사실로 인정하려면 Gemini grounding URL이 최소 하나 있어야 합니다.
- 생성 프롬프트에는 선택된 claim만 남기며, 미선정 숫자와 비수치 주장도 제거합니다.
- flow의 3점 근거는 관계 키워드가 아니라 코드가 계산한 유의미한 결합값의 실제 사용입니다.

### 비용·시간

- Claude Batch는 명시적으로 `USE_BATCH=1`일 때만 켜집니다.
- 기본 50건 실행은 `GEN_STAGE_SIZE=100`이고 목표를 채우면 나머지 후보를 생성하지 않습니다.
- 장전에는 전일 마감 후 저장한 동일 기준일 시세 캐시가 충분하면 전종목 siseJson 조회를 생략합니다.
- Claude 동기 작성은 `CLAUDE_SYNC_WORKERS=6`의 제한된 병렬 호출입니다.
- `run_stats.jsonl`에서 다음 필드를 보면 절감량을 확인할 수 있습니다.

| 필드 | 의미 |
|---|---|
| `generation_candidates` | 생성 전 확보한 전체 후보 |
| `generation_attempted` | 실제 LLM 생성 묶음에 들어간 후보 |
| `generation_items_avoided` | 목표 조기 달성으로 생성하지 않은 후보 |
| `judge_failed` | 정규식 통과 후 심사 자체가 실패한 글 |
| `delivery_attempted` / `delivery_failed` | 텔레그램 전송 대상과 실패 수 |
| `attempt_to_delivery_yield` | 실제 생성 시도 대비 전송 성공률 |
| `by_kind_funnel` | 유형별 수집·차단·시도·필터·심사·전송 퍼널 |

## 검증 결과

푸시 직전 `./run_tests.sh` 전체를 실행하는 것이 완료 조건입니다. 현재 추가된 회귀 범위는
다음과 같습니다.

- 단위 회귀 267건: 심사 fail-closed, 전송 성공분, DART 접수번호, derived fact,
  provider 장애, 시세 캐시, 날짜별 dedup, 게시판 매핑, RSS 신선도, poll 질문, 퍼널 계측
- 설정·프롬프트 전수 감사: 실패 0
- 계약 조합 감사: 실패 0 (기존 설명성 경고 3건은 별도 관찰)
- E2E 20건: 소스·LLM·텔레그램 경계만 mock하고 게이트부터 배포 판정까지 실제 코드 사용

실행 명령:

```bash
./run_tests.sh
git status --short
git log --oneline 275acbe..HEAD
```

## 첫 운영 실행에서 볼 항목

1. `[claude] batch` 로그가 기본 실행에서 없어야 합니다.
2. `[market] 장 시작 전 확정 캐시 ... 사용`이 나오면 2,600종목 fallback 조회가 없어야 합니다.
3. Gemini 일시 429는 재시도하되, 결제 오류가 난 뒤에는 후속 Gemini 호출이 없어야 합니다.
4. `judge_failed`는 배포 수에 포함되면 안 됩니다.
5. `delivery_failed=0`인지, 실패 시 성공분만 `state.json`에 들어갔는지 확인합니다.
6. `generation_items_avoided`가 0보다 크면 staged generation이 실제 비용을 절감한 것입니다.
7. `by_kind_funnel`에서 flow의 `attempted→delivered`와 `fit` 평균을 함께 봅니다.
8. 정책 공급이 급감하면 48시간 신선도 제외 로그를 먼저 확인합니다.

## 의도적으로 남긴 관찰 사항

- rubric 변경 전후의 `fit` 평균은 같은 척도가 아닙니다. 운영상 새 `fit`은 기록하되,
  품질 개선 판정은 동일 benchmark와 담당자 승인율·수정량으로 해야 합니다.
- 휴장일 fallback은 최대 4일 전 확정 캐시만 허용하며, 글의 `기준일`은 캐시 원래 날짜를 유지합니다.
- 사건일을 찾을 수 없는 항목은 `_undated` event 버킷을 씁니다. 실제 로그에서 정상 반복
  공시가 막히면 해당 소스에 발간일을 추가해야 하며, 다시 광범위한 dedup 완화부터 하면 안 됩니다.
- 정책 RSS는 발행 시각이 없으면 fail-closed입니다. 특정 정상 feed가 전부 빠지면 그 feed의
  날짜 태그를 `_feed_date()`에 명시적으로 추가합니다.
- poll은 질문형 문체 한 종만 사용합니다. 하루 배포 상한이 2건이므로 계약 일관성을 다양성보다
  우선했습니다. 다양성을 늘리려면 질문 계약을 가진 별도 페르소나를 만든 뒤 감사를 통과시켜야 합니다.

## 롤백

각 변경은 위 표처럼 독립 커밋으로 분리했습니다. 문제가 특정되면 해당 커밋만 `git revert`하고,
여러 변경을 한꺼번에 되돌리지 않습니다. 데이터 파일과 사용자 운영 기록은 코드 롤백 대상으로
간주하지 않습니다.
