# 문장틀 문구 교정 인수인계 (2026-09-12)

## 기준 커밋

- 시작: `3f45e7489536a068dab4565cffee00962cf322e3`
- 코드 변경: `03113c4943eafee229b08ed356a2eb2e6987f57b`

## 확인한 문제

GitHub Actions [34695901713](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34695901713)은
테스트방 ACK 5/5, 문장틀 보충 1건을 기록했습니다. 해당 본문은 수치와 방향은 정확했지만
`기준일 마감 등락률`, `누적 등락률은 ... 상승`, `마감 가격은 ...이었네요`처럼 운영
데이터를 그대로 읽는 느낌이 강했습니다. 구조·사실성 검사는 통과했지만 자연스러운 문장인지
검사하는 회귀 기준은 없었습니다.

## 커밋별 변경 + 불변식

| 커밋 | 변경 | 불변식 |
|---|---|---|
| `03113c4` | `_CHANGE`, `_CLOSE`, 세 관계 문장 표현을 자연어로 교체하고 운영체·비문 회귀 검사 1건 추가 | renderer 결정성, 원문+`template_id` 재구성, 주장 최대 3개, 결합 사실 사용, 65건 reserve, 22종·동일 틀 최대 3건, LLM 호출 수를 바꾸지 않음 |

## 동작 변경

- `기준일 종가 기준`, `거래량 배수`, `종가의 마감 위치`, `누적 값` 같은 내부 지표형
  표현을 `직전 거래일보다`, `20일 평균 대비 거래량`, `장중 고가 대비 종가` 표현으로
  바꿨습니다.
- `종가는 ... 종가는 ...`처럼 인접 문장이 같은 주어로 시작하던 조합을 정리했습니다.
- 라우팅, 생성량, 심사, 전송, API 호출에는 변경이 없습니다. 따라서 이 커밋만으로 비용이
  줄었다고 판단하지 않습니다.

## 검증 결과

| 검증 | 결과 |
|---|---:|
| 기존 단위 회귀 | 340/340 |
| 문장틀 계약 | 13/13 |
| 설정·프롬프트 감사 | FAIL 0 / WARN 0 |
| Persona×Angle 계약 감사 | 268/268 |
| E2E | 20/20 |
| 실제 시세 캐시 234종목 | reserve 65/65, 전건 유효, 22종 사용 |

## 첫 실행에서 볼 항목

1. 테스트 모드 5건 ACK와 `template_fallback_count`를 확인합니다.
2. 문장틀 카드가 포함되면 본문을 전수 검토해 내부 지표형 표현과 인접 반복이 없는지 봅니다.
3. `llm_usage.calls`, token 비용, LLM/문장틀 발송 비율을 이전 실행과 분리해 기록합니다.
4. 5건이 정상이면 테스트방 50건으로 확대하고, 품질과 비용을 함께 비교합니다.

## 의도적으로 남긴 것

- Flash-Lite rejection-only, 프롬프트 캐싱은 이번 문구 교정과 한 변수로 묶지 않았습니다.
- 문장틀 다양성 구조와 페르소나 배분은 바꾸지 않았습니다.
- 최신 5건 비용 `$0.275161`은 Sonnet 심사 5회 감소가 포함된 실행 변동값이므로, 문구
  교정 효과로 귀속하지 않습니다.

## 롤백

문구 교정만 되돌릴 때 `03113c4`를 revert합니다. 이전의 문장틀 reserve와 fail-closed 검증,
LLM 우선 배분은 유지됩니다.
