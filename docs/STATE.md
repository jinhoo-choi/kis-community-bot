# 현재 상태

> **작업 시작 전 이 파일을 먼저 읽습니다.** 작업 종료 시 갱신합니다.
> 협업 규칙은 [`AI_COLLABORATION.md`](AI_COLLABORATION.md) 참조.

**최종 갱신:** 2026-09-14 / Claude (`#121` 퍼널 진단 · 비-flow 공급 3종 수정 병합 · 실행 대기)

**기능 기준:** `ce9ae84` (PR #18 · #19 병합 완료)

**마지막 성공 실발송:** Actions `#121` **50/50** (2026-09-14 06:11 정기 실행, `09a2add` 기준)

**Claude 문서 기준:** [`HANDOFF_2026-09-14_NONFLOW_SUPPLY_FIX.md`](HANDOFF_2026-09-14_NONFLOW_SUPPLY_FIX.md)

**현재 작업 상태:** 코드 작업 완료, **실행 대기**. `#121` 이 50/50 로 성공했으나 퍼널이
flow 48건(96%)으로 쏠려, 원인 3건을 PR #18 로 수정·병합했습니다. 병합 후 `main` 전체
회귀는 단위 380/380, 문장틀 14/14, 감사 FAIL 0 / WARN 0, 계약 FAIL 0 / WARN 0,
E2E 20/20 이고 `pr-tests` CI 는 success 입니다. 수정 후 실행은 하지 않았습니다 —
Telegram suffix 가 만료된 상태에서 돌리면 비용만 반복됩니다(`#112` 에서 `$0.195939`).
남은 작업은 전부 사용자 액션이 선행 조건입니다(아래 "막혀 있는 것"). 평가 도구는
병합됐지만 `judge-eval-104` 는 아직 한 번도 실행하지 않았습니다. 외부 모델로 104건을
전송하고 최대 208회 유료 호출하는 작업이므로 사용자의 명시 승인 전에는 실행하지
않습니다.

**공동 검토 결과:** Claude의 품질 조건과 Codex의 비용·라우팅 조건을 문서 8항으로
조율했습니다. Claude는 3차 배분에서 flow 절대상한을 목표 미달분에 한해 풀었고,
Codex는 호출·토큰·비용 계측, fresh-first 재작성, rescue-only 보강, 동적 생성 stage와
22종 flow 문장틀 reserve를 순서대로 반영했습니다. 비용 0~4단계는
[`HANDOFF_2026-09-12_CODEX_COST_IMPLEMENTATION.md`](HANDOFF_2026-09-12_CODEX_COST_IMPLEMENTATION.md),
reserve와 최신 실발송은
[`HANDOFF_2026-09-12_TEMPLATE_RESERVE_VALIDATION.md`](HANDOFF_2026-09-12_TEMPLATE_RESERVE_VALIDATION.md),
문장틀 문구 검토는
[`HANDOFF_2026-09-12_TEMPLATE_COPY_REVIEW.md`](HANDOFF_2026-09-12_TEMPLATE_COPY_REVIEW.md)에
있습니다. 최신 5건·50건 비용과 품질 전수검토는
[`HANDOFF_2026-09-12_COST_VALIDATION.md`](HANDOFF_2026-09-12_COST_VALIDATION.md)에 있습니다.

---

## 현재 결론

`#121`(2026-09-14 06:11 정기 실행)은 **50/50 발송 성공**입니다. 전송 실패 0, 심사 실패 0.
다만 유형 퍼널이 flow 48건(96%)이었고 비용은 `$1.509993` / 309콜 / 건당 `$0.0302` 로
직전 50건(`#110`, 건당 `$0.023044`) 대비 31% 늘었습니다. 비-flow 가 죽은 자리를 flow
재생성으로 메우면서 생성 시도가 218건까지 늘어난 결과입니다.

| 유형 | 수집 | 게이트차단 | 생성시도 | 필터통과 | 보류 | 발송 |
|---|---:|---:|---:|---:|---:|---:|
| flow | 197 | 0 | 154 | 54 | 71 | **48** |
| research | 52 | 5 | 35 | **29** | **29** | **0** |
| disclosure | 48 | 21 | 26 | 8 | 6 | 2 |
| policy | 12 | **11** | 1 | 0 | 0 | 0 |
| poll | 4 | 0 | 2 | 0 | 0 | 0 |

원인 3건을 PR #18 로 수정하고, 점검 중 추가로 찾은 리포트 가짜 주장 문제를 PR #19 로 수정했습니다. 상세는
[`HANDOFF_2026-09-14_NONFLOW_SUPPLY_FIX.md`](HANDOFF_2026-09-14_NONFLOW_SUPPLY_FIX.md).

| # | 파일 | 문제 | 근거 |
|---|---|---|---|
| 1 | `src/claims.py` | research 는 `target`·`opinion` 이 `terms` 앵글 5~6순위라 `n=2~3` 에 잘리고 리포트 제목만 남았다 | filter_passed 29건 전건 보류, 사유가 `fit` 1~2 '제목 재진술' 16건 |
| 2 | `src/filters.py` | `facts.py` 가 넣은 `전일 종가 대비` 를 쓴 본문을 filters 가 리젝 — 룰 충돌 | 정규식 리젝 1위 `flow:상대날짜` 60건 전건 |
| 3 | `main.py` | rescue 트리거가 총량 기준이라 flow 가 채우면 비-flow 보강이 0회 | policy 12건 중 11건 `tier5` 사망, rescue 0회 |
| 4 | `src/claims.py`, `src/gate.py` | 목표가·투자의견이 **전무한** 리포트가 `용어 설명: 투자의견은 …` 주입 덕에 게이트를 통과하고, ※ 주석 줄에서 `opinion = 미제공. 추정하지 말 것.` 이라는 가짜 주장이 생겼다. 1번 앵커가 이 가짜 값을 고정시켜 **단독으로는 악화**된다 | 보류 사유 '용어설명 반복만', fatal '평가 주체를 밝히지 않은 간접 평가' |

심사 rubric 은 바꾸지 않았습니다. 전후 점수가 같은 척도가 아니게 되고 104건 평가와
변수가 겹칩니다. 리서치 `사실성 3/5` 축 조정은 평가 판정 이후 과제로 남습니다.

`tools/audit_contracts.py` 가 2번 충돌을 못 잡은 점은 별도로 남깁니다. 감사 범위가
전역 규칙 × 페르소나 × Angle 이라 **facts 생성 코드와 필터 사이의 충돌**은 대상
밖입니다. 같은 종류가 더 있을 수 있습니다.

---

## 이전 결론 (2026-09-12 기준)

테스트 텔레그램 방 실발송과 후속 검증을 진행했습니다. 2026-09-12에
`50건 + 5건 + 5건 + 5건 + 5건`을 다섯 번 발송했고 모두 전송 실패와 심사 실패가 0건이었습니다.
첫 대량 실행에서 확인한 Gemini 출력 중단, 보강 호출 낭비, 심사 오판과 실제 문구 문제를
순서대로 보정한 뒤 소량 재실행 네 번으로 회귀와 비용 계측을 확인했습니다.

앞선 문구 문제는 `3e5abb9`, 비용 계측 실행에서 추가로 발견한 고저 차이·마감·
거래량 표현은 `3a64a99`에서 보정했습니다. 이미 보낸 다섯 본문은 새 정제기와
필터에 그대로 재입력해 5/5 통과함을 확인했습니다. 새 문장틀 경로도 테스트방에서 1건
사용됐고, 이때 확인한 내부 지표형 문구를 `03113c4`에서 교정했습니다.

페르소나 생성 흐름 재점검에서 과거 실발송에도 나타난 `quick_memo × context` 계약 우회를
확인해 `fa39dff`에서 차단했습니다. 짧은메모는 공시에만 활성화하고, 다음 실행부터
`by_persona`에 페르소나별 시도·통과·보류·발송·점수·Angle 분포를 기록합니다. 상세 구조와
전체 변경 이력은 [`PERSONA_GENERATION_PROCESS.md`](PERSONA_GENERATION_PROCESS.md)에 있습니다.

비용 절감 0~4단계와 문장틀 보장 경로의 실발송 검증까지 끝났습니다. 교정판 5건은 호출
`55→40회`, 건당 token 비용 `$0.055032→$0.039921`로 약 27.5% 줄었고, 확대 실행은
Telegram 50/50, 호출 235회, 총 `$1.152185`, 건당 `$0.023044`였습니다. 최초 50건 기준선
최소 459회보다 호출이 48.8% 감소했습니다. 검색 보강·grounding query와 전송 실패는 0건,
LLM 37건과 문장틀 13건이 발송됐습니다.

50건 전수검토에서는 flow 48건 편중과 함께 Sonnet이 놓친 장중 순서·기술적 신호 추정 4건,
문장틀 인접 반복 1건을 확인했습니다. `e8113df`에서 해당 추정을 필터로 막고 고저차·거래량
비문을 결정형으로 정리했습니다. 전체 회귀는 단위 345/345, 문장틀 14/14, 감사 실패·경고 0,
계약 268/268, E2E 20/20입니다.

---

## 실발송 검증 결과

| 실행 | GitHub Actions | 수집 | 후보 | 생성 시도 | 필터 통과 | 보류 | 발송 | 실패 | 최종 유형 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| 50건 본검증 | [34681905042](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34681905042) | 351 | 279 | 200 | 104 | 54 | 50 | 심사 0 / 전송 0 | 공시 4 / 특징주 46 |
| 1차 후속 | [34684279029](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34684279029) | 49 | 42 | 20 | 9 | 4 | 5 | 심사 0 / 전송 0 | 공시 1 / 특징주 4 |
| 최종 후속 | [34684869099](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34684869099) | 49 | 42 | 20 | 12 | 7 | 5 | 심사 0 / 전송 0 | 정책 1 / 특징주 4 |
| 비용계측 후 5건 | [34693897268](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34693897268) | 49 | 43 | 43 | 17 | 12 | 5 | 심사 0 / 전송 0 | 특징주 5 |
| 문장틀 적용 후 5건 | [34695901713](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34695901713) | 49 | 43 | 43 | 12 | 27 | 5 | 심사 0 / 전송 0 | 발제 1 / 특징주 4 |
| 교정판 5건 | [34698613554](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34698613554) | 49 | 43 | 30 | 9 | 24 | 5 | 심사 0 / 전송 0 | 발제 1 / 특징주 4 |
| 비용 절감 50건 | [34698837940](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34698837940) | 351 | 304 | 156 | 79 | 94 | 50 | 심사 0 / 전송 0 | 공시 1 / 정책 1 / 특징주 48 |

`#107` 문장틀 적용 실행의 주요 확인값:

- 검색 보강과 grounding query는 모두 0건이었습니다.
- stage는 `[20, 23]`, 새 후보 43건을 모두 썼고 filter rewrite는 0회였습니다.
- LLM 호출은 Haiku 작성 34, Gemini 작성 9, Sonnet 심사 12로 총 55회였습니다.
- 입력 190,551 / 출력 7,274 token, token 비용은 `$0.275161`였습니다.
- 실제 발송은 Claude 3, Gemini 1, 문장틀 1건이었습니다.
- 원문 수치는 5/5 일치했지만 고저 차이를 상승폭처럼 읽히게 하는 표현과 중복 마감 문장을
  발견해 `3a64a99`로 정제·회귀 테스트를 보강했습니다.
- 첫 50건 실행에서 279개 후보가 확보됐으므로 현재 총 후보량 자체는 부족하지 않습니다.
  병목은 공시·리서치·정책·발제의 필터/심사 통과율과 유형 균형입니다.

최신 `#109`·`#110` 확인값과 전수검토 후 보정은
[`HANDOFF_2026-09-12_COST_VALIDATION.md`](HANDOFF_2026-09-12_COST_VALIDATION.md)를 기준으로 합니다.

---

## 날짜 확인

실행일 `2026-09-12`는 월요일이 아니라 **토요일**입니다. 다섯 실발송에서 사용한 시세와
DART 기준 사실은 금요일 `2026-09-11` 자료였으며, 금·토·일 자료를 합쳐 만든 결과가 아닙니다.

월요일 `2026-09-14` 경로는 별도 회귀 테스트로 고정했습니다.

| 소스 | 월요일 기준 범위 |
|---|---|
| DART | 금요일~일요일 (`2026-09-11`~`2026-09-13`) |
| 정책 RSS | 최근 72시간 |
| 시세 | 직전 거래일인 금요일 (`2026-09-11`) |

실제 월요일 공급량은 `2026-09-14 06:11 KST` 정기 실행의 유형별 퍼널로 최종 확인해야 합니다.

---

## 모델 대안 상태

| 대안 | 현재 상태 | 다음 확인 |
|---|---|---|
| 유료 Gemini → 무료 Gemini | 별도 무료 프로젝트 키와 `gemini-3.1-flash-lite`로 작성·심사를 넘기는 경로 구현 및 테스트 완료 | `GEMINI_FREE_API_KEY_TEST`를 등록하고 유료 결제 오류를 강제해 실제 호출 검증 |
| Claude | Haiku 작성, Sonnet 5 우선 심사 사용 중 | 실제 운영 비용과 담당자 수정률 관찰 |
| ChatGPT/OpenAI API | 키·어댑터·비용 정책이 없어 미반영 | 무료 Gemini가 운영량/품질을 못 채울 때 3번째 독립 provider로 추가 검토 |
| GitHub PAT | 저장소·Actions 제어용일 뿐 OpenAI API 인증으로 사용할 수 없음 | 모델 호출 대안으로 사용하지 않음 |

무료 Gemini 실호출은 해당 Secret이 현재 저장소에 없어서 아직 검증하지 못했습니다.
유료 Gemini는 후속 실행에서 4개 중 3개가 결정형 필터를 통과해, 초기 `0/39`보다
출력 완성도가 회복됐습니다.

---

## 후보 추가 방안

후보 총량은 279건으로 50건 목표보다 충분했습니다. 따라서 오래된 날짜나 품질 낮은
특징주를 더 넣지 않고, 부족한 유형의 **구조화된 사실 밀도**를 높이는 순서로 확장합니다.

1. 리서치: 제목·목표가만 쓰지 말고 접근 가능한 초록/본문의 핵심 근거를 추출하되,
   원문 링크·발간일·종목코드·이용 조건을 함께 보존합니다.
2. 공시: DART/KIND의 접수번호에 결합되는 구조화 필드를 공시 유형별로 늘립니다.
3. 정책: 종목·섹터 연결 근거가 명시된 기사만 유지하고 정책 본문 핵심 수치를 구조화합니다.
4. 운영: 유형별 `attempted→filter_passed→delivered` 수율과 담당자 수정률을 함께 보고
   가장 낮은 단계부터 보완합니다.

---

## 검증 기준

- 기존 단위 회귀: `353/353`
- 문장틀 보장 계약: `14/14`
- 설정·프롬프트 감사: 실패 `0`, 경고 `0`
- 계약 조합 감사: 실패 `0`, 경고 `0`
- E2E: `20/20`
- 마지막 다섯 실발송 본문 재생 검증: `5/5` 정제·필터 통과
- 실제 시세 캐시 문장틀: reserve `65/65`, 보장 모드 `50/50`
- 런타임 Persona×Angle 표본: `4,200/4,200` 호환

---

## API 비용 절감 공동 조율안 (0~4단계 + reserve 구현 완료)

API 비용 절감안을 [`PROPOSAL_2026-09-12_COST_REDUCTION.md`](PROPOSAL_2026-09-12_COST_REDUCTION.md)에
Claude 제안, Codex 검토, Claude 2차 회신과 최종 조율안을 모두 기록했습니다. 실행 기준은
문서 **8항**입니다.

| 항목 | 확정 내용 |
|---|---|
| 기준선 | 50건당 애플리케이션 호출 최소 459회 |
| 우선 절감 | 즉시 재작성 제거 → 후단 보강 → 최초 60·후속 10~60 stage |
| 심사 | Flash-Lite는 탈락 선별 전용, LLM 최종 승인은 Sonnet |
| template | flow 한정, 평시 15건·보장 모드 부족분 최대 50건 |
| 다양성 | 문장틀 22종 이상, 동일 문장틀 3건 이하 |
| 문체상한 | 보장 모드에서 템플릿 페르소나 3종 각각 17건 |
| 50건 | 1·2차 구성 목표 뒤 3차에서 flow 절대상한 해제 |
| 구현 | `7c5d241` → `bb03ecf` → `ac456f1` → `86dc9d1` → `9fca3af` → `4c5da44` |

3차 flow 상한 완화와 template reserve를 결합해, 날짜가 유효한 검증 시세 원본 65건이
있으면 모든 LLM이 중단돼도 50개 본문을 준비할 수 있습니다. Telegram 자체 장애 중 실제
수신까지 보장하지는 않으며 ACK 50 미만은 계속 workflow 실패로 처리합니다.

## 협업 방식 (2026-09-13 확정)

`daily.yml`이 매 영업일 06:11에 `main` 코드 그대로 실행되므로, 발송 경로 코드의 `main`
직접 푸시는 곧 운영 실행입니다. 문서·테스트는 `main` 직접, 발송 경로 코드·`config.py`·
워크플로·라우팅은 PR로 나눕니다. 자기 PR 자기 머지는 하지 않고, 실채널 전환 시점부터는
코드 전건 PR로 올립니다. 상세는 [`AI_COLLABORATION.md`](AI_COLLABORATION.md) 3b항입니다.

PR 회귀 CI(`pr-tests.yml`)는 PR로 올려 둔 상태이며, 병합 전까지 PR은 자동 검증 없이
열립니다. 그 전 PR은 작업자가 `./run_tests.sh` 결과를 PR 본문에 적습니다.

## 공시 게이트 차단 원인 규명 (완료)

차단 23건은 **과차단이 아니라 거래소 수시공시(접수번호 8 대역)라 정형 API 대상이 아니어서**
facts 에 수치가 안 붙은 것이었습니다. 원문에서 값을 뽑을 수 있는 2종(유형자산취득·영업양수)은
`dart_detail.DOC_TYPES` 로 살렸고(차단 23 → 21, 상세 보강 26/48건), 나머지는 표 구조가 다르거나
정정·후속 보고라 살리지 않는 것으로 판단했습니다. 전 과정 LLM 호출 0(dry-run + DART 무료 API).

근거와 판단은 [`HANDOFF_2026-09-13_DART_GATE_SUPPLY.md`](HANDOFF_2026-09-13_DART_GATE_SUPPLY.md)에
있습니다. `tools/probe_dart_endpoints.py` 는 남겨 두었습니다 — 표본이 없던 유형이 잡히는 날
다시 돌리면 라벨을 확인할 수 있습니다.

## 막혀 있는 것 — 사용자 액션 선행

| 항목 | 필요한 것 | 이후 진행 |
|---|---|---|
| 테스트 채널 50건 실행 | `@commwrite_bot`에 새 메시지 전송 → suffix 갱신 | PR #18 의 세 수정 효과를 한 실행에서 분리 측정: research·policy `delivered`, `reject_reasons["flow:상대날짜"]`, `generation_attempted`, `llm_usage.calls`, `enrich.CALLS` |
| 104건 심사 평가 | 외부 전송·유료 호출 명시 승인 | Flash-Lite 채택 gate 판정 |
| 리서치 심사 `사실성 3/5` 8건 | 위 평가 판정 | 심사 축 조정 착수 |

## 프롬프트 캐싱 — 현 구조로는 이득이 없다 (2026-09-13 Claude 분석)

제안 문서 9.3의 전제(입력의 95%가 고정)가 실제 코드와 맞지 않았습니다. 시스템 프롬프트에
**항목별 `claim_block`이 들어 있어** 호출마다 프리픽스가 달라집니다. 이 상태로
`cache_control`만 붙이면 전건이 cache write(입력 단가 1.25배)가 되어 비용이 오릅니다.

| 구간 | 길이(자) | 성격 |
|---|---:|---|
| `rule_block` + 공통 문체 | 2,855 | 전 호출 동일 |
| 페르소나·앵글 계약 | 266 | persona×angle 조합별 |
| `claim_block` | 351 | **항목별** |

캐싱을 살리려면 고정 구간을 프롬프트 앞으로 옮기고 가변 구간을 뒤로 보내는
재배치가 필요합니다. 이는 생성 프롬프트의 순서를 바꾸는 일이라 품질 튜닝 전체가 영향을
받고, 검증에는 유료 실행이 필요합니다. 절감 기대치는 현재 비용($0.023/건 기준) 대비
월 몇 달러 수준이라 **지금은 착수하지 않습니다.** 실채널 전환 후 물량이 늘면 재검토합니다.

## 병합 완료된 PR

`#110`(50건) 퍼널을 유형별로 분해한 결과, flow 96% 편중의 원인은 유형 상한이 아니라
**비-flow 유형이 게이트·필터·심사에서 죽는 것**이었습니다. 진단과 조치를 PR로 나눴습니다.

| 유형 | 수집 | 게이트차단 | 생성시도 | 필터통과 | 보류 | 발송 |
|---|---:|---:|---:|---:|---:|---:|
| flow | 234 | 0 | 104 | 60 | 77 | 48 |
| research | 52 | 5 | 27 | 12 | **12** | **0** |
| disclosure | 49 | **23** | 19 | 6 | 5 | 1 |
| policy | 12 | 5 | 5 | 1 | 0 | 1 |

| PR | 내용 | 근거 |
|---|---|---|
| [#5](https://github.com/jinhoo-choi/kis-community-bot/pull/5) | `pr-tests.yml` — PR 회귀 CI | `pull_request` 트리거가 없어 PR 자동 검증이 0이었음 |
| [#6](https://github.com/jinhoo-choi/kis-community-bot/pull/6) | facts 날짜 인용을 근거없는수치로 리젝하지 않음 | 정규식 리젝 1위 `research:근거없는수치['11']` 10건 + 공시 1건 |
| [#7](https://github.com/jinhoo-choi/kis-community-bot/pull/7) | 게이트 차단을 `filter_log`에 건별 기록 (측정 전용) | 차단 23건의 제목·facts가 남지 않아 과차단 판단 불가 |
| [#8](https://github.com/jinhoo-choi/kis-community-bot/pull/8) | 공시는 금액 주장(`issue_amt`)을 항상 선정 | 공시 보류 5건 전부 `커뮤니티적합성 2/5`, 코멘트에 금액 누락 반복 |

병합은 **#5 → #6 → #7 → #8** 순으로 끝났습니다(`61d2171`). #6과 #8은 같은 `src/claims.py`를 건드리지만
수정 지점이 달라 충돌하지 않습니다. #6은 Codex 리뷰 지적(날짜 형식만 검사 → `2026-99-77`의 `99`가 시점으로 풀림)을
반영해 숫자 경계와 `datetime.date()` 실존 검증을 추가한 뒤 병합됐습니다(`b90d188`). 상세 진단은 #6에 포함된
`docs/HANDOFF_2026-09-13_NONFLOW_SUPPLY.md`에 있습니다.

**보류 1건**: 리서치 심사 `사실성 3/5` 보류 8건은 심사 축 정의를 바꾸는 일이라 Flash-Lite
평가와 변수가 겹칩니다. 104건 평가 판정 이후에 착수합니다.

## 다음 작업

0. (완료) `pr-tests.yml` 병합. 이후 코드 PR은 CI 초록일 때만 병합합니다.
1. 사용자가 과거 게시글·facts 104건의 외부 모델 전송과 최대 208회 유료 호출을 명시
   승인한 뒤에만 `judge-eval.yml`을 한 번 실행합니다.
2. 모든 채택 gate를 확인하고, 전부 통과할 때만 Flash-Lite rejection-only 라우팅을 별도
   PR로 구현합니다. 하나라도 실패하면 운영 전환을 보류합니다.
3. 사용자가 `@commwrite_bot`에 새 메시지를 보낸 뒤 `verify.yml`에서 suffix `2744` ACK를
   먼저 확인하고, 성공한 경우에만 테스트 채널 5건 운영 회귀를 실행합니다.
4. 프롬프트 캐싱은 보류합니다. 사유는 위 "프롬프트 캐싱" 절에 있습니다.
5. #5~#8 병합 후 테스트 채널 50건을 한 번 실행합니다. 세 수정의 효과와 게이트 차단
   23건의 정체(`filter_log`의 `gate_blocked` 행)가 같은 실행에서 함께 나옵니다.
   실행 전후로 `by_kind_funnel`의 research·disclosure `delivered`를 비교합니다.
6. Batch는 60분 workflow·단계 중단과 충돌하므로 현재 비활성 상태를 유지합니다.

---

## 알려진 외부 제약

| 소스 | 상태 |
|---|---|
| 네이버 시세 순위 페이지 | 개편으로 사용 불가. siseJson과 지정일 전 종목 일봉으로 랭킹 생성 |
| 네이버 리서치 HTML | 개편으로 사용 불가. `front-api/research/*` 사용 |
| KRX `data.krx.co.kr` | `LOGOUT` 응답 |
| 한경컨센서스 | 정상 |
| DART / KIND | 정상 |

시세 순위는 장 시작 전 비어 있을 수 있으므로 동일 기준일 확정 캐시를 우선 사용합니다.
정확한 기준일 데이터와 캐시가 모두 없을 때만 최대 4일 이내 최근 확정 캐시로 폴백합니다.

---

## 운영 설정 요약

| 항목 | 값 |
|---|---|
| 정기 실행 | 평일 06:11 KST (`daily.yml`) |
| 시세 캐시 적재 | 평일 20:30 KST (`verify.yml`) |
| 목표 발송 | 50건 |
| 배포 상한 | 공시 12 / 리포트 12 / 특징주 19 / 정책 6 / 발제 2 |
| 배포 절대상한 | 특징주 30 (목표×0.6). 1·2차 고정, 3차에서 미달분만 해제(경고 로그) |
| 문장틀 reserve | flow 65건 선준비; 평시 최대 15건, 보장 모드에서 부족분 보충 |
| LLM 생성 목표 | reserve 정상이면 35건; 미달이면 50건 |
| 생성 묶음 | 최초 60건, 이후 누적 실수율 기준 10~60건 |
| 검색 보강 | 후보 부족 시 5건씩; 성공 캐시 7일 / NONE 1일 / 오류 미저장 |
| 담당자 | 7명 (`data/assignees.json`) |
| Gemini 무료 폴백 | 별도 무료 프로젝트의 `gemini-3.1-flash-lite`; 작성·심사만 사용 |
