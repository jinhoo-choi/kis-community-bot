# 비용 절감 5건·50건 실측 인수인계 (2026-09-12)

## 결론

비용 절감 0~4단계와 문장틀 reserve를 함께 적용한 정상 실행에서 목표 수량을 유지하면서
호출과 token 비용이 줄었습니다. 다만 50건 전수검토에서 Sonnet 심사를 통과한 flow 본문
4건의 근거 없는 장중 순서·기술적 해석과 문장틀 1건의 인접 반복을 발견해 `e8113df`에서
추가 차단했습니다.

## 실행 결과

| 실행 | 결과 | 호출 | token 비용 | 건당 비용 | LLM / 문장틀 |
|---|---:|---:|---:|---:|---:|
| [#107 기존 5건](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34695901713) | 5/5 | 55 | `$0.275161` | `$0.055032` | 4 / 1 |
| [#108 교정판 1차](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34698242084) | 0/5 | 27 | `$0.135806` | 산출 불가 | 준비 3 / 2 |
| [#109 교정판 5건](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34698613554) | 5/5 | 40 | `$0.199606` | `$0.039921` | 3 / 2 |
| [#110 확대 50건](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34698837940) | 50/50 | 235 | `$1.152185` | `$0.023044` | 37 / 13 |

`#108`은 저장된 `TELEGRAM_TEST_CHAT_ID`가 비어 있는데 dispatch의 임시 suffix도 빠져 실제
발송이 차단된 실행입니다. 생성 비용은 발생했으므로 비용 합계에서 숨기지 않되, 정상 경로의
건당 비용 비교에서는 제외합니다. `#109`, `#110`은 비공개 테스트 채널 suffix 매칭과 전송
실패 0건을 확인했습니다.

정상 5건끼리 비교하면 호출과 건당 비용이 각각 약 27% 줄었습니다. 50건에서는 최초 기준선의
최소 459회 대비 235회로 48.8% 감소했습니다. 과거 50건 실행에는 token 계측이 없으므로
`$1.152185`를 과거 실측 비용과 직접 비교하지 않습니다.

## 50건 상세

- 생성 stage는 `[60, 60, 36]`, 실제 생성 시도 156건, 미호출 후보 148건입니다.
- 신규 검색 보강과 grounding query는 0건입니다.
- 작성 156회, Sonnet 심사 79회로 총 235회입니다.
- 최종 발송은 Claude 30, Gemini 7, 검증 문장틀 13건입니다.
- 평균 심사점수는 `14.81/20`, 심사 실패와 Telegram 전송 실패는 모두 0건입니다.
- 유형은 공시 1, 정책 1, flow 48건입니다. 50건 수량은 달성했지만 flow 96% 편중은 별도
  품질·공급 과제로 남습니다.

## 전수검토 후 보정

| 발견 | 처리 |
|---|---|
| `코칩이 거래량 ...` 및 저가 대비 상승폭·고저차 어순 | 사실값은 바꾸지 않고 결정형 정제 |
| OHLC만으로 `마감을 앞두고`, `당일 후반부` 순서 추정 | `claim_out_of_scope`로 생성·필터 차단 |
| 거래량이 가격을 뒷받침하거나 모멘텀 약화를 시사한다는 해석 | `claim_out_of_scope`로 생성·필터 차단 |
| 문장틀 `마감 가격은 ... 마감 가격은 ...` | `flow_template_13` 관계문 주어 교체 |

`#110`의 37개 LLM 본문을 새 코드로 재검사하면 8건은 안전한 문법 정제로 고쳐지고, 근거 없는
해석 4건은 차단됩니다. 단위 회귀 `345/345`, 문장틀 계약 `14/14`, 감사 FAIL 0/WARN 0,
Persona×Angle 계약 268/268, E2E 20/20을 통과했습니다.

## Flash-Lite 평가 주의

과거 104건 고정 표본은 [#102](https://github.com/jinhoo-choi/kis-community-bot/actions/runs/34681905042)의
결정형 필터 통과본입니다. 당시 저장된 50개 발송본은 전부 `judged_by=gemini`였으므로, 기존
`score`나 `fatal`을 Sonnet 정답으로 간주하면 안 됩니다. 같은 104건의 원문 facts와 본문을
고정한 뒤 **Sonnet과 Flash-Lite를 둘 다 다시 호출**해 혼동행렬을 만들어야 합니다.

채택 기준은 기존 합의대로 Sonnet fatal 재현율 80% 이상, Sonnet 통과 false reject 15% 이하,
50건 준비량 불변, Sonnet staged-only 대비 건당 비용 20% 이상 절감입니다. 네 조건을 모두
확인하기 전에는 Flash-Lite를 운영 라우팅에 연결하지 않습니다.

## 다음 순서

1. `e8113df`와 이 문서를 원격 반영하고 테스트방 5건으로 새 필터의 운영 회귀를 확인합니다.
2. 과거 104건 facts를 고정해 Sonnet·Flash-Lite 쌍대 평가를 실행합니다.
3. 고정 prefix 분리와 1건 warm-up 뒤 prompt cache read token을 실측합니다.
4. Batch는 최대 24시간 계약이 현재 60분 workflow와 충돌하므로 계속 비활성화합니다.
