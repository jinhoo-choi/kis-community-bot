# 오탐 / 품질 이슈 이력

운영 중 발견된 오탐·품질 이슈를 축적하는 회귀 테스트 기록.
새 이슈 발견 시 이 표에 행을 추가하고, 수정 커밋 해시와 회귀 검증 방법을 남긴다.
로직 변경 시 이 표의 기존 사례가 다시 재현되지 않는지 `./run_tests.sh` 로 확인한다.

| 날짜 | 유형 | 증상 | 근본원인 | 수정 커밋 | 회귀 검증 |
|---|---|---|---|---|---|
| 2026-09-03 | 종목 오귀속 | (예방적 수정) "대상 기업 실적 점검" 같은 제목이 상장사 `대상`(001680)으로 매핑되어 엉뚱한 종목방으로 배포될 수 있었음 | `tickers.resolve()` 가 최장일치 정규식만 쓰고 귀속검증 절차가 없었음. 인사이트봇 2026-08-02 "삼성"→삼성증권 오승격과 동일 구조 | (본 커밋) | `entity.verify_attribution()` 신설. `AMBIGUOUS_NAMES` 는 주체 신호(조사·행위어) 동반 필수. tests 4케이스 |
| 2026-09-03 | 부수 언급 오탐 | (예방적 수정) "○○ 밸류체인 점검 - 협력사 XX" 리포트가 협력사 종목방으로 배포될 수 있었음 | 종목명이 본문에 등장하면 무조건 주체로 인정 | (본 커밋) | `entity.is_incidental()` — **fail-open** 설계. 인사이트봇 2026-08-24 교훈(반대로 설계 시 조사 생략 제목이 과잉 드롭)을 반영 |
| 2026-09-03 | 중복 생성 | (예방적 수정) 같은 유상증자 건이 DART·리서치·수급 3개 소스로 들어와 3건 생성됨 | dedup 이 `item["id"]` 단일 축 | (본 커밋) | `dedup.keys()` 다축(ID/EVT/TTL) + 제목 유사도. 조회·저장이 같은 함수를 공유해 인사이트봇 2026-08-02 "저장부 정규화 누락" 재발 차단 |
| 2026-09-03 | 조용한 수집 실패 | (예방적 수정) 네이버·한경 셀렉터가 개편되면 예외 없이 0건이 되어 몇 주간 리서치 슬롯이 비어도 알 수 없었음 | 단일 셀렉터 하드코딩 + 실패 시 빈 리스트 반환 | (본 커밋) | `crawl.select_rows()` 셀렉터 배열 폴백 + `crawl.report()` 헬스체크. 0건·30% 미만 시 텔레그램 운영 경고 |

| 2026-09-03 | **크래시** | `ENABLE_JUDGE=1` + 프로바이더 2개일 때 `judge_all()` 이 `KeyError: '"factual"'` 로 죽음 | `judge.SYSTEM` 이 출력 예시로 JSON 리터럴 `{"factual":n,...}` 을 품고 있는데 `.format()` 이 이 중괄호를 치환 필드로 해석. 프로바이더가 1개면 조기 return 되어 드러나지 않았음 | (본 커밋) | `.format()` → `.replace("__FATAL_BLOCK__", ...)`. 단위 테스트에 "프롬프트 빌드 무예외" 추가. **E2E 시뮬레이션이 발견** |
| 2026-09-03 | dedup 무효화 | 같은 실행에서 DART와 리서치가 같은 사건을 들고 오면 둘 다 통과 | `filter_new()` 가 과거 실행분(`seen`)만 조회하고 **같은 배치에서 통과한 항목의 키를 등록하지 않음.** 배치 내부 중복은 제목 유사도 축으로만 걸려 표현이 다르면 통과 | (본 커밋) | 배치 로컬 `batch` dict 병합 조회. `seen` 은 오염시키지 않음. E2E `[S4]` 케이스 |

| 2026-09-03 | 헬스체크 구멍 | DART·pykrx·정책RSS 가 전부 실패해도 운영 경고가 뜨지 않음 | `crawl.report()` 가 research 소스에만 붙어 있었음. 나머지는 그냥 빈 리스트를 반환 | (본 커밋) | 전 소스에 `report()` 적용. **dry-run 실행 중 발견** |
| 2026-09-03 | 경고 오진 | API 소스(DART) 장애에도 "셀렉터 개편 의심" 문구가 출력 | `report()` 가 사유를 받지 않고 문구를 하드코딩 | (본 커밋) | `reason` 파라미터 추가. `expected=0` 은 정상 0건(휴장·키 미설정)으로 구분해 경고 제외 |

| 2026-09-03 | 한경 전건 강등 | 한경 리포트 4건이 **전부** 종목 매핑 실패로 자유게시판행. 제목에 `롯데지주(004990)` 처럼 코드가 있는데도 못 잡음 | 파서 결함 3개 — (1) `td[7]` 차트링크의 `business_code` 미추출 (2) 제목 셀 span 중복으로 텍스트가 2배 (3) `tds[-2]/[-1]` 을 작성자/출처로 읽어 빈 문자열 | (본 커밋) | 실구조 진단 후 재작성. `_undouble()` + `_strip_code()` 회귀 3케이스 |
| 2026-09-03 | 소스 전건 실패 | `pykrx` 가 `KRX 로그인 실패: KRX_ID/KRX_PW 미설정` 로 market 0건. 연쇄로 `tickers.listed()` 도 빈 dict → **모든 소스의 종목 매핑이 무력화** | pykrx 가 KRX 계정을 요구하도록 변경됨. 단일 라이브러리에 상장리스트와 시세를 모두 의존 | (본 커밋) | 상장리스트 → KRX KIND `corpList.do`, 시세 → 네이버 `sise_quant`. pykrx 의존 제거 |
| 2026-09-03 | 지역 IP 차단 | korea.kr·기재부·금융위·산업부·금감원 전건 `ConnectTimeout`. 로컬에서는 403, Actions 에서는 타임아웃으로 증상이 달라 오진했음 | 정부 사이트가 해외 IP 를 차단. GitHub Actions 러너는 미국 IP | (본 커밋) | 진단에서 생존한 연합뉴스 RSS 로 대체. **정부 원문이 필요하면 국내 IP self-hosted runner 가 유일한 해법** |
| 2026-09-03 | 진단 오진 | 한경 4건을 "제목-종목 어긋남(데이터 오염)"으로 판단했으나 실제로는 파서 결함이었음 | 로그의 표본만 보고 원인을 추정. 실 HTML 을 확인하지 않았음 | (본 커밋) | `tools/diagnose.py` 신설 — 추정 전에 실구조를 덤프하고 커밋해서 본다 |

| 2026-09-03 | 한경 전건 미매핑 | 한경 리포트 4건이 전부 종목 태그 없이 테마글로 강등 | 파서 결함 3개: ① 차트 링크의 `business_code` 미추출 ② 제목 셀 span 중복으로 텍스트가 2배 ③ `tds[-2]`/`tds[-1]`을 작성자/출처로 읽어 빈 문자열 유입 | (본 커밋) | 실측 HTML 기준 재작성. `_undouble()`/`_strip_code()` 회귀 4케이스 |
| 2026-09-03 | market 전건 실패 | `KRX 로그인 실패: KRX_ID 또는 KRX_PW 환경 변수가 설정되지 않았습니다` | pykrx 가 KRX 계정을 요구하도록 변경됨. 로컬 개발 시엔 캐시로 가려짐 | (본 커밋) | pykrx 의존 제거. 네이버 `sise_quant` 로 교체(진단 HTTP 200/83건). 상장리스트는 KRX KIND `corpList.do` |
| 2026-09-03 | 연쇄 실패 | pykrx 실패 → `listed()` 빈 dict → **모든 소스가 테마글로 강등**되는데 `attr_reject` 가 비어 원인 추적 불가 | 상장리스트 로드 실패와 귀속 검증 실패가 같은 경로로 처리됨 | (본 커밋) | `상장리스트없음` / `종목명미검출` 사유 분리 |
| 2026-09-03 | policy 전건 실패 | korea.kr 4개 피드 전부 ConnectTimeout | GitHub Actions 해외 IP 차단으로 추정. **로컬에서는 재현되지 않음** | (본 커밋) | 연합뉴스 RSS로 대체(진단 HTTP 200). korea.kr 은 optional 로 유지해 국내 러너 전환 시 자동 복구 |

| 2026-09-03 | 상장리스트 0건 | KRX KIND `corpList.do` 가 HTTP 200 / 1.26MB 를 반환했는데 파싱 결과 0종목 | `html.parser` 가 1.2MB 급 표 중간에서 트리를 끊음 | (본 커밋) | `lxml` 우선 + 정규식 폴백 + 1,000종목 미만이면 경고 |
| 2026-09-03 | 정치 소재 유입 | 연합뉴스 RSS 로 `추미애 1차 추경`, `박홍근 청년 투자`, `연합뉴스 이시각 헤드라인` 등이 수집됨 | korea.kr 대체로 종합 뉴스 RSS 를 쓰면서 정부 보도자료 성격 필터가 없었음 | (본 커밋) | `gate` tier3 에 정치 키워드 추가 + policy 에 `EXCLUDE`/`POLICY_SIGNALS` 이중 조건. 회귀 2케이스 |

### 알려진 의도적 동작 (버그 아님)
| 동작 | 판단 |
|---|---|
| 같은 종목의 공시 실적 + 리포트 실적전망이 `EVT::{code}::실적` 로 병합되어 1건만 남음 | 하루에 같은 회사 실적 글이 2건 나가는 것보다 1건이 낫다. `EVENT_TYPES` 의 실적 버킷이 넓은 것은 의도 |
| 귀속 검증 실패 시 드롭이 아니라 테마글 강등 | 종목방만 틀렸을 뿐 글감 자체는 유효할 수 있음 |

## 다음 이슈 발견 시 기록 방법
1. 텔레그램 배포분에서 증상 확인, 또는 직원이 `반려` 누른 글 확인
2. `data/filter_log_*.json` 에서 해당 건의 판정 사유 대조
3. 수정 후 `tests/` 에 재현 케이스 추가 → `./run_tests.sh` 로 기존 케이스 회귀 확인
4. 이 표에 행 추가 → 커밋

## 설계 원칙 (실패에서 얻은 것)
| 원칙 | 근거 |
|---|---|
| **미탐 > 오탐** | 커뮤니티 게시는 애매한 글을 안 올리는 게 이득. 리스크 모니터링(오탐>미탐)과 반대 |
| **귀속 판정은 fail-open** | 인사이트봇에서 반대로 설계했더니 조사 생략 제목이 과잉 드롭됨 |
| **dedup 조회·저장은 같은 함수** | 조회부에만 정규화를 적용해 dedup 이 영구 미스된 사례 |
| **수치 기반 dedup 축 금지** | "N억원 돌파"류 정규식은 재보도에도 매번 매칭되어 억제를 뚫음 |
| **조용한 0건이 예외보다 위험** | 예외는 로그에 남지만 0건은 정상처럼 보인다 |
| **추정하지 말고 덤프해서 본다** | 한경 문제를 로그 표본만 보고 오진했다. `tools/diagnose.py` 로 실구조를 먼저 본다 |
| **환경별로 증상이 다르다** | 같은 차단이 로컬에선 403, Actions 에선 ConnectTimeout 으로 나타났다 |
| **한 라이브러리에 두 가지를 의존하지 않는다** | pykrx 하나가 죽으면서 시세와 상장리스트가 동시에 날아갔다 |
| **규칙은 단일 소스** | 프롬프트 이중 관리로 한쪽만 수정돼 규칙이 무력화된 사례 |
